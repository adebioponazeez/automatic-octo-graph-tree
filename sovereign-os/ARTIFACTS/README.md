# ARTIFACTS — v30

> Filed output of approved work. Write artifacts into the store the same day (Workflow A step 8).
> Each artifact must carry a `META` + `HANDOFF` footer (see `PLAYBOOKS/HANDOFF.md`).

## Naming
`ARTIFACTS/YYYY-MM-DD--<type>--<slug>.md`
Types: `strategy`, `architecture`, `content`, `sop`, `audit`, `report`.

## Index
| Date | File | Operator | Type | CRITIC verdict |
|---|---|---|---|---|
| | | | | |
